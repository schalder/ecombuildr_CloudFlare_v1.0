project_id = "fhqwacmokbtbspkxjixf"

# Disable JWT verification for public image transformation
# This allows anonymous users to load optimized images without authentication
[functions.image-transform]
verify_jwt = false

# Disable JWT verification for Stripe Connect callback
# Stripe OAuth redirects don't include auth headers, so callback must be public
# POST requests (initiation) will handle JWT verification manually in code
[functions.stripe-connect]
verify_jwt = false